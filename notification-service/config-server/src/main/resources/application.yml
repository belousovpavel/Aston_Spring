spring:
  application:
    name: config-server

  cloud:
    config:
      server:
        git:
          uri: ${CONFIG_REPO_URI:https://github.com/yourusername/config-repo}
          default-label: main
          search-paths: '{application}'
          clone-on-start: true
        default-application: application
        prefix: /config

server:
  port: